<Window x:Class="WpfApp.TrailerDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:WpfApp.Views"
        xmlns:controls="clr-namespace:WpfApp.Controls"
        mc:Ignorable="d"
        Title="TrailerDetailsWindow" Height="550" Width="800">
    <Grid Margin="10">
        <StackPanel>
            <controls:TrailerDetailsControl Trailer="{Binding Trailer}" />

            <Grid Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Content="Save" Click="SaveButton_Click" Grid.Column="0" Margin="5"/>
                <Button Content="Cancel" Click="CancelButton_Click" Grid.Column="1" Margin="5"/>
            </Grid>
        </StackPanel>
    </Grid>
</Window>